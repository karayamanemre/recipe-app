<tr>
  <th scope="row"><%= index + 1 %></th>
  <td><%= food.name %></td>
  <td><%= food.measurement_unit %></td>
  <td>$<%= food.price %></td>
  <td class="text-center"><%= button_to "Delete", food_path(food), method: :delete, class: "btn btn-danger" %></td>
</tr>
